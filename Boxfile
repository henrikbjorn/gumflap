web1:
  name: Gumflap
  document_root: web
  php_version: 5.4.14
  shared_writable_dirs:
    - /config
    - /vendors
  php_extensions:
    - apc
    - zip
    - curl
    - pdo_mysql
  after_build:
      - "if [ ! -f composer.phar ]; then curl -s http://getcomposer.org/installer | php; fi; php composer.phar -q install -o"
